server:
  port: 0
spring:
  datasource:
    url: jdbc:mysql://mysql-list-repair-service:3308/listrepair
    username: mysqluser
    password: adminpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://eureka-service:8761/eureka
  instance:
    instance-id: eureka-service:${spring.application.instance_id:${random.value}}
