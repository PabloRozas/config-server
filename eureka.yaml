server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    healthcheck:
      enabled: true
    lease:
      renewalIntervalInSecs: 60
      durationInSecs: 10
    eviction-interval-timer-in-ms: 30000
    service-url:
      default-zone: http://eureka-service:8761/eureka/
